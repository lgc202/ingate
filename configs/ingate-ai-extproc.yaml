server:
  grpc:
    addr: "127.0.0.1:18081"
  http:
    addr: "127.0.0.1:18082"
    timeout: "5s"
  shutdown_timeout: "30s"

data:
  apiserver:
    master: ""
    kubeconfig: "_output/kubeconfig"

logging:
  format: "json"
  level: "info"
  add_source: false
