server:
  http:
    addr: "0.0.0.0:8001"
    timeout: "30s"
  console_dir: "/opt/ingate/console/web"
  shutdown_timeout: "30s"

data:
  admin_api:
    base_url: "http://admin-api:8002"

logging:
  format: "json"
  level: "info"
  add_source: false
